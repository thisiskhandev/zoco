import { GenericModel } from "@shared/generic-model";

export enum ExtraChargesEnum {
    title = 'Extra Charges',

    hasExtraCharges = 'hasExtraCharges',
    extraCharges = 'extraCharges',

    valueOne = 1,
    valueZero = 0,

    getErrorMessage = 'There was an error trying to get the Extra Charges',
}
