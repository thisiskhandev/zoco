import { GenericModel } from "@shared/generic-model";

export enum ExtraChargesPricesEnum {
    title = 'Extra Charges Prices',
    noCharges = 'No charge was selected',
    extraCharges = 'extraCharges',
    price = 'priceCharge',

    valueZero = 0,
    numberMaxSize = 5,
}
